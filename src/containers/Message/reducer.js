/*
 *
 * MyChannel reducer
 *
 */

// import { DEFAULT_ACTION } from './constants';

export const initialState = { messageData: {}, inProcess: false };

/* eslint-disable default-case, no-param-reassign */
export default function setBrowserInfo(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
